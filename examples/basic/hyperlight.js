export default {
  esbuild: (config) => {
    // not necessary, here for demonstrative purposes. If you omit the esbuild paramater
    // from the configuration hyperlight will just use default configs, this here is just a
    // placeholder it actually just returns the default config
    return config
  }
}
